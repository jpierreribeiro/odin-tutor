`for i in 1 ..= 4` runs the body four times, with `i` taking each value.

The lesson is not the answer. It is that you can step BACKWARDS through the
loop and watch `total` and `i` take the values they took, one step at a time.

A hardcoded 10 has the right value and nothing to step through.
