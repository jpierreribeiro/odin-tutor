`Student{name = "Ana", age = 20}` builds ONE value holding two fields.

In the OBJECTS panel a struct appears once, with its fields listed underneath by
name. Two loose variables appear in FRAMES as two separate slots with no
relationship between them.

Reach a field with a dot: `student.age`.
