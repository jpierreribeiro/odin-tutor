A variable holds whatever you last assigned to it.

`total := 0` declares it AND gives it a value. Change that value to the one the
exercise asks for, and both the picture and the printed output follow.

Watch the FRAMES panel: before the declaring line, `total` reads
`- not yet`. That is not zero. It is "this does not exist yet", and the two are
different facts.
