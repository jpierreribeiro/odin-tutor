# Probe report — 2026-08-05

Generated by `probes/run.sh`. SPEC-TEST-041.

This file is evidence for a row in the compatibility table
([PLATFORM-SUPPORT.md](../../docs/PLATFORM-SUPPORT.md) §5). A row is added only
when the suite has run and this report is committed (SPEC-PLAT-031).

## Combination

| | |
|---|---|
| Odin | `dev-2026-07-nightly:819fdc7` |
| Debugger | GNU gdb (Ubuntu 15.0.50.20240403-0ubuntu1) 15.0.50.20240403-git |
| Platform | Linux x86_64 |
| Build flags | `odin build <file> -file -debug` |

## Results

| Probe | Class | Result | Evidence |
|---|---|---|---|
| `entry-symbol` | BLOCKING | **pass** | main::main resolves to main::main |
| `line-table` | BLOCKING | **pass** | student lines visited, in order: [9, 10, 11, 12, 13, 14] |
| `struct-fields` | BLOCKING | **pass** | fields=['name', 'marks', 'age'] age=20 |
| `slice-fields` | BLOCKING | **pass** | marks={len:3 values:[7, 8, 9]} sub={len:2} sub.data - marks.data = 8 bytes (shared storage, detectable from the observation alone) |
| `string-value` | HIGH | **pass** | read as "Ana" (len=3) |
| `frame-key-spread` | HIGH | **pass** | 25 invocations produced 11 distinct (caller pc, sp) keys across 3 call sites. Fewer keys than invocations is correct: a key must be unique among frames that are live AT THE SAME TIME, and a returned frame's stack slot is reused by the next call from the same site. |
| `only-student-code` | HIGH | **fail, expected** | 54 of 60 plain `step` stops landed outside scalars.odin (90%). A failure here is expected and is why SPEC-ADP-014 exists. |
| `frame-key` | HIGH | **pass** | depth 7, caller key (pc, sp) = (0x555555594003, 0x7fffffffd340), unchanged after a step within the invocation: True |
| `finish-breakpoint` | HIGH | **pass** | 25 return values observed of 25 invocations, 0 wrong, 0 out of scope. Sample (n, return): [(1, 1), (0, 0), (2, 1), (1, 1), (3, 2), (1, 1), (0, 0), (2, 1), (4, 3), (1, 1)] |
| `simple-return` | HIGH | **pass** | double(21) observed as [42] (expected [42]) |
| `thread-count` | HIGH | **pass** | 1 thread(s) in a plain program |
| `thread-event` | HIGH | **pass** | extra threads seen by event: [2], after 3 steps, at line 23 |
| `thread-count-is-unsound` | HIGH | **pass** | the per-stop thread count never rose above 1, while the event caught 1. A safeguard built on the count would never fire, which is worse than none — its presence implies protection. |
| `map-entries` | HIGH | **fail, expected** | type=struct map[string]int exposes ['data', 'len', 'allocator']. len=2. No entry access. R-20 stands: a map shows its count and marks its entries unknown. |
| `confined-stepping` | HIGH | **pass** | 7 student steps, 1 escapes by `finish` |
| `free-symbol` | MEDIUM | **pass** | breakpoint-able: runtime::heap_free, runtime::mem_free, runtime::heap_allocator_proc, free |
| `no-debug-info` | MEDIUM | **pass** | lookup_global_symbol('main::main') on a stripped build returned None. That is the DEBUG_INFO_MISSING check: gdb itself does not fail, so the adapter must detect this and name the error. |

## `step-cost`, at three sizes

Recorded with the SPEC-ADP-014 confinement in place, so this is the cost
of a step the student actually sees, not of a raw `step`.

| Target | Student steps | ms per step |
|---|---|---|
| `scalars` | 7 | 6.25 |
| `fibonacci` | 78 | 1.75 |
| `long-trace` | 400 | 1.21 |

## Failures that are not news

**`only-student-code`** — Expected. 35% of stops landed outside the student's source on 2026-08-05. The mitigation is SPEC-ADP-014.

**`map-entries`** — Expected. R-20: the map type gives no entry access. Until that decision is made, a map shows its count and marks its entries unknown.

## What a reader should take from this run

Every BLOCKING probe passed, and nothing failed that was not already
known to fail.

A probe failure is a supported-platform failure, not a bug
(SPEC-TEST-042). It means this combination is not supported. It does not
mean the tool is broken.

